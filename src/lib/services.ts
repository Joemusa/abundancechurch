// Edit this list to match your actual service schedule, then redeploy
// (or push to GitHub if Vercel auto-deploys from your repo).
export const SERVICES = ["Sunday Morning", "Sunday Evening", "Wednesday Bible Study"] as const;
